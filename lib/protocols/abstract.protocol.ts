// import type { PackageDefinition } from '@grpc/proto-loader';

// import { GrpcResponse } from './interfaces';

export abstract class AbstractProtocol {
  // public abstract invokeUnaryRequest(packageDefinition: PackageDefinition): Promise<GrpcResponse>;
}
